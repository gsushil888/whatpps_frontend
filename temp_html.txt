<div class="bg-white dark:bg-gray-800">
  <!-- Filters -->
  <div class="flex gap-2 px-4 py-3 border-b border-gray-200 dark:border-gray-700 overflow-x-auto scrollbar-hide">
    <button *ngFor="let filter of filters" (click)="selectFilter(filter)"
      class="px-3 py-1 rounded-full text-sm font-medium transition-colors whitespace-nowrap"
      [class.bg-green-500]="activeFilter === filter" [class.text-white]="activeFilter === filter"
      [class.bg-gray-100]="activeFilter !== filter" [class.dark:bg-gray-700]="activeFilter !== filter"
      [class.text-gray-700]="activeFilter !== filter" [class.dark:text-gray-300]="activeFilter !== filter">
      {{ filter }}
    </button>
    <button (click)="showContactsList()"
      class="px-3 py-1 rounded-full text-sm font-medium transition-colors whitespace-nowrap"
      [class.bg-green-500]="showContacts" [class.text-white]="showContacts" [class.bg-gray-100]="!showContacts"
      [class.dark:bg-gray-700]="!showContacts" [class.text-gray-700]="!showContacts"
      [class.dark:text-gray-300]="!showContacts">
      Contacts
    </button>
    <button (click)="showCreateGroupView()"
      class="px-3 py-1 rounded-full text-sm font-medium transition-colors whitespace-nowrap"
      [class.bg-green-500]="showCreateGroup" [class.text-white]="showCreateGroup" [class.bg-gray-100]="!showCreateGroup"
      [class.dark:bg-gray-700]="!showCreateGroup" [class.text-gray-700]="!showCreateGroup"
      [class.dark:text-gray-300]="!showCreateGroup">
      <i class="fas fa-users mr-1"></i> New Group
    </button>
  </div>

  <!-- Conversations List -->
  <div *ngIf="!showContacts && !showCreateGroup">
    <div *ngFor="let chat of chats"
      class="flex items-center gap-3 px-4 py-3 border-b border-gray-100 dark:border-gray-700 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
      (click)="selectChat(chat.id)" [class.bg-green-50]="chat.id === selectedChatId"
      [class.dark:bg-green-900]="chat.id === selectedChatId" [class.border-l-4]="chat.id === selectedChatId"
      [class.border-l-green-500]="chat.id === selectedChatId">
      <div class="relative">
        <img [src]="chat.avatar" class="w-12 h-12 rounded-full object-cover cursor-pointer" alt="avatar" (click)="openAvatarModal(chat.avatar, $event)" />
        <div *ngIf="isUserOnline(chat)"
          class="absolute -bottom-1 -right-1 w-4 h-4 bg-green-500 rounded-full border-2 border-white dark:border-gray-800">
        </div>
      </div>

      <div class="flex-1 min-w-0">
        <div class="flex items-center justify-between mb-1">
          <h3 class="font-semibold text-gray-900 dark:text-gray-100 truncate">
            {{ chat.name }}
          </h3>
          <span class="text-xs text-gray-500 dark:text-gray-400 flex-shrink-0 ml-2">
            {{ chat.date }}
          </span>
        </div>
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-1 min-w-0 flex-1 overflow-hidden">
            <i *ngIf="chat.lastMessageType === 'IMAGE'" class="fas fa-image text-gray-500 text-xs flex-shrink-0"></i>
            <i *ngIf="chat.lastMessageType === 'VIDEO'" class="fas fa-video text-gray-500 text-xs flex-shrink-0"></i>
            <i *ngIf="chat.lastMessageType === 'AUDIO'"
              class="fas fa-microphone text-gray-500 text-xs flex-shrink-0"></i>
            <i *ngIf="chat.lastMessageType === 'DOCUMENT'"
              class="fas fa-file-alt text-gray-500 text-xs flex-shrink-0"></i>
            <p class="text-sm text-gray-600 dark:text-gray-300 truncate min-w-0 flex-1">
              <!-- Group chat: show sender first name as prefix -->
              <span *ngIf="chat.type === 'GROUP' && chat.lastMessageSenderName" class="text-gray-500 font-medium">
                {{ chat.lastMessageSenderId === currentUserId ? 'You' : chat.lastMessageSenderName.split(' ')[0] }}: 
              </span>
              <!-- Individual chat: show 'You:' only if current user sent it -->
              <span *ngIf="chat.type !== 'GROUP' && chat.lastMessageSenderId === currentUserId" class="text-gray-500">You: </span>
              <span *ngIf="chat.lastMessageType !== 'TEXT'" class="text-gray-500">
                {{ getMediaTypeLabel(chat.lastMessageType || 'TEXT') }}
              </span>
              <span *ngIf="chat.lastMessageType === 'TEXT'">{{ chat.lastMessage }}</span>
            </p>
          </div>
          <div class="flex items-center gap-1 flex-shrink-0 ml-2">
            <div *ngIf="chat.unreadCount > 0"
              class="min-w-[20px] h-5 bg-green-500 rounded-full flex items-center justify-center px-1">
              <span class="text-xs text-white font-medium">{{ chat.unreadCount > 99 ? '99+' : chat.unreadCount }}</span>
            </div>
          </div>
        </div>
        <!-- Online status text -->
        <div *ngIf="chat.type === 'INDIVIDUAL' && getLastSeenText(chat)" class="mt-1">
          <span class="text-xs text-green-500" *ngIf="isUserOnline(chat)">online</span>
          <span class="text-xs text-gray-400" *ngIf="!isUserOnline(chat)">{{ getLastSeenText(chat) }}</span>
        </div>
      </div>
    </div>

  <!-- Profile Image Modal -->
  <div *ngIf="selectedAvatarUrl" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-80" (click)="closeAvatarModal()">
    <img [src]="selectedAvatarUrl" class="max-w-xs max-h-[80vh] rounded-xl object-contain" (click)="$event.stopPropagation()" />
  </div>

    <div *ngIf="chats.length === 0"
      class="flex flex-col items-center justify-center py-12 text-gray-500 dark:text-gray-400">
      <i class="fas fa-comments text-4xl mb-4 text-gray-300 dark:text-gray-600"></i>
      <p class="text-lg font-medium">No chats yet</p>
      <p class="text-sm">Search for contacts to start chatting</p>
    </div>
  </div>

  <!-- Contacts List -->
  <div *ngIf="showContacts && !showCreateGroup">
    <div *ngFor="let contact of contacts"
      class="flex items-center gap-3 px-4 py-3 border-b border-gray-100 dark:border-gray-700 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
      (click)="selectContact(contact)">
      <div class="relative">
        <img [src]="contact.profilePictureUrl || 'assets/google.svg'" class="w-12 h-12 rounded-full object-cover"
          alt="avatar" />
        <div *ngIf="contact.isOnline"
          class="absolute -bottom-1 -right-1 w-4 h-4 bg-green-500 rounded-full border-2 border-white dark:border-gray-800">
        </div>
      </div>

      <div class="flex-1 min-w-0">
        <h3 class="font-semibold text-gray-900 dark:text-gray-100 truncate">
          {{ contact.displayName }}
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-300 truncate">
          {{ contact.phoneNumber }}
        </p>
      </div>
    </div>

    <div *ngIf="contacts.length === 0"
      class="flex flex-col items-center justify-center py-12 text-gray-500 dark:text-gray-400">
      <i class="fas fa-address-book text-4xl mb-4 text-gray-300 dark:text-gray-600"></i>
      <p class="text-lg font-medium">No contacts yet</p>
      <p class="text-sm">Add contacts to start chatting</p>
    </div>
  </div>

  <!-- Create Group -->
  <div *ngIf="showCreateGroup" class="h-full flex flex-col">
    <!-- Step 1: Select Participants -->
    <div *ngIf="createGroupStep === 'select'" class="flex flex-col h-full">
      <div class="p-3 border-b dark:border-gray-700">
        <div class="flex items-center justify-between mb-2">
          <h3 class="font-semibold text-gray-900 dark:text-gray-100">Add participants</h3>
          <button (click)="cancelCreateGroup()" class="text-gray-500 hover:text-gray-700 dark:hover:text-gray-300">
            <i class="fas fa-times"></i>
          </button>
        </div>
        <input type="text" placeholder="Search contacts" [(ngModel)]="searchTerm"
          class="w-full px-4 py-2 bg-gray-100 dark:bg-gray-700 rounded-lg outline-none text-sm" />
      </div>

      <div *ngIf="selectedContacts.length > 0"
        class="px-3 py-2 border-b dark:border-gray-700 flex gap-2 overflow-x-auto">
        <div *ngFor="let contact of selectedContacts" class="flex flex-col items-center gap-1 min-w-[60px]">
          <div class="relative">
            <img [src]="contact.profilePictureUrl || 'assets/google.svg'" class="w-12 h-12 rounded-full object-cover" />
            <button (click)="removeSelectedContact(contact)"
              class="absolute -top-1 -right-1 w-5 h-5 bg-gray-500 rounded-full flex items-center justify-center text-white hover:bg-gray-600">
              <i class="fas fa-times text-xs"></i>
            </button>
          </div>
          <span class="text-xs text-gray-700 dark:text-gray-300 truncate w-full text-center">{{
            contact.displayName.split(' ')[0] }}</span>
        </div>
      </div>

      <div class="flex-1 overflow-y-auto">
        <div *ngFor="let contact of filteredGroupContacts"
          class="flex items-center gap-3 px-4 py-3 border-b border-gray-100 dark:border-gray-700 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700"
          (click)="toggleContactSelection(contact)">
          <div class="relative">
            <img [src]="contact.profilePictureUrl || 'assets/google.svg'" class="w-12 h-12 rounded-full object-cover" />
            <div *ngIf="isContactSelected(contact)"
              class="absolute -bottom-1 -right-1 w-5 h-5 bg-green-500 rounded-full flex items-center justify-center border-2 border-white dark:border-gray-800">
              <i class="fas fa-check text-white text-xs"></i>
            </div>
          </div>
          <div class="flex-1">
            <h3 class="font-semibold text-gray-900 dark:text-gray-100">{{ contact.displayName }}</h3>
            <p class="text-sm text-gray-500 dark:text-gray-400">{{ contact.phoneNumber }}</p>
          </div>
        </div>
      </div>

      <div class="p-3 border-t dark:border-gray-700">
        <button (click)="nextToGroupDetails()" [disabled]="selectedContacts.length === 0"
          class="w-full py-2 bg-green-500 text-white rounded-lg hover:bg-green-600 disabled:opacity-50 disabled:cursor-not-allowed">
          Next ({{ selectedContacts.length }})
        </button>
      </div>
    </div>

    <!-- Step 2: Group Details -->
    <div *ngIf="createGroupStep === 'details'" class="flex flex-col h-full">
      <div class="p-4 border-b dark:border-gray-700">
        <div class="flex items-center gap-3 mb-4">
          <button (click)="backToSelectParticipants()" class="text-gray-600 dark:text-gray-300">
            <i class="fas fa-arrow-left"></i>
          </button>
          <h3 class="font-semibold text-gray-900 dark:text-gray-100">New group</h3>
        </div>
      </div>

      <div class="flex-1 overflow-y-auto p-4">
        <div class="flex justify-center mb-4">
          <label class="cursor-pointer relative">
            <input type="file" accept="image/*" class="hidden" (change)="onGroupImageSelect($event)" #groupImageInput />
            <div
              class="w-20 h-20 bg-gray-200 dark:bg-gray-700 rounded-full flex items-center justify-center overflow-hidden">
              <img *ngIf="groupImagePreview" [src]="groupImagePreview" class="w-full h-full object-cover" />
              <i *ngIf="!groupImagePreview" class="fas fa-camera text-2xl text-gray-400 dark:text-gray-500"></i>
            </div>
          </label>
        </div>

        <div class="mb-4">
          <input type="text" placeholder="Group name (required)" [(ngModel)]="groupName" maxlength="255"
            class="w-full px-4 py-2 bg-gray-100 dark:bg-gray-700 rounded-lg outline-none text-sm border border-transparent focus:border-green-500" />
        </div>

        <div class="mb-4">
          <textarea placeholder="Group description (optional)" [(ngModel)]="groupDescription" maxlength="1000" rows="2"
            class="w-full px-4 py-2 bg-gray-100 dark:bg-gray-700 rounded-lg outline-none text-sm border border-transparent focus:border-green-500 resize-none"></textarea>
        </div>

        <div class="text-sm text-gray-600 dark:text-gray-400 mb-2">Participants: {{ selectedContacts.length }}</div>
        <div class="flex flex-wrap gap-2">
          <div *ngFor="let contact of selectedContacts"
            class="flex items-center gap-2 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">
            <img [src]="contact.profilePictureUrl || 'assets/google.svg'" class="w-6 h-6 rounded-full object-cover" />
            <span class="text-sm">{{ contact.displayName }}</span>
          </div>
        </div>
      </div>

      <div class="p-3 border-t dark:border-gray-700">
        <button (click)="createGroup()" [disabled]="!groupName.trim() || isCreatingGroup"
          class="w-full py-2 bg-green-500 text-white rounded-lg hover:bg-green-600 disabled:opacity-50 disabled:cursor-not-allowed">
          {{ isCreatingGroup ? 'Creating...' : 'Create Group' }}
        </button>
      </div>
    </div>
  </div>
</div>